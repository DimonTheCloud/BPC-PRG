[project]
name = "exercise-07-acc-activity"
version = "0.1.0"
description = "Detection of activity from accelerometer data"
readme = "README.md"
requires-python = ">=3.11"
dependencies = ["pytest"]

[tool.pytest.ini_options]
pythonpath = ["."]

